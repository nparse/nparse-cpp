script:
    - calc_eq/Calc1.ng
    - calc_eq/Calc2.ng
    - calc_eq/Calc3.ng

config:
    input_pool:        5120
    lr_threshold:      0

test1:
    input:             1 ? 2 ? 3 ? 4 ? 5 ? 6 ? 7 ? 8 = 50
    traces:            1
    check:
        guess:         1 * 2 * 3 * 4 + 5 + 6 + 7 + 8 = 50

test2:
    input:             1 ? 2 ? 3 ? 4 ? 5 = 5 ? 4 ? 3 ? 2 ? 1
    traces:            20

